<div class="container tess">
Number of user = <%= @users.count %>

<br>

User names:
<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.email %>    
    </li>
  <% end %>    
</ul>
</div>

<%= link_to "Home", root_path, class: 'btn btn-primary' %>