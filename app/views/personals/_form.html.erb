<%= form_with(model: personal, local: true) do |form| %>
  <% if personal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(personal.errors.count, "error") %> prohibited this personal from being saved:</h2>

      <ul>
      <% personal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :personal_name %>
  </div>

  <div class="field">
    <%= form.label :Date %>
    <%= form.date_select :Date, id: :personal_Date %>
  </div>

  <div class="field">
    <%= form.label :point %>
    <%= form.number_field :point, id: :personal_point %>
  </div>

  <div class="field">
    <%= form.label :money %>
    <%= form.text_field :money, id: :personal_money %>
  </div>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_area :desc, id: :personal_desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
