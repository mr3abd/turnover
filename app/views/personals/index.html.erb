<p id="notice"><%= notice %></p>

<h1><%= t :pe %></h1>

<table>
  <thead>
    <tr>
      <th><%= t :name %></th>
      <th>Date</th>
      <th>Point</th>
      <th>Money</th>
      <th>Desc</th>
      <th>Test ! </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @personals.each do |personal| %>
      <tr>
        <td><%= personal.name %></td>
        <td><%= personal.Date %></td>
        <td><%= personal.point %></td>
        <td><%=  personal.money.round %></td>
        <td><%= personal.desc %></td>
        <td><%= personal.money * personal.point %> </td>
        <td><%= link_to 'Show', personal %></td>
        <td><%= link_to 'Edit', edit_personal_path(personal) %></td>
        <td><%= link_to 'Destroy', personal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<h1><%= t :hello %></h1>

<br>

<%= link_to 'New Personal', new_personal_path %>
